// TODO: (will be solved in another task) decide if we need a legacy version of the plugin
//    - if yes, then imprort polyfills here before the export
//    - else, remove legacy related code (webpack config, scripts in package.json, this file)
export { default } from './plugin';
