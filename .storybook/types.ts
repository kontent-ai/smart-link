export enum PositionProperty {
  Absolute = 'absolute',
  Fixed = 'fixed',
  Relative = 'relative',
  Static = 'static',
  Sticky = 'sticky',
}

export enum OverflowProperty {
  Auto = 'auto',
  Hidden = 'hidden',
  Scroll = 'scroll',
  Visible = 'visible',
}

